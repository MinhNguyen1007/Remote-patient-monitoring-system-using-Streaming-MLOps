[pytest]
testpaths = .
# Các module đánh số theo thứ tự chạy (xem docstring test_1_streaming_pipeline.py); không chạy song song.
addopts = -p no:randomly --strict-markers -ra
log_cli = true
log_cli_level = INFO
log_cli_format = %(asctime)s %(levelname)s %(message)s
log_cli_date_format = %H:%M:%S
