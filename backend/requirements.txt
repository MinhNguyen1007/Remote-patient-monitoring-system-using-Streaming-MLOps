# Cài từ gốc repo: pip install -r backend/requirements.txt
# Giai đoạn D mới cần phần DB (schema + migration); FastAPI/JWT bổ sung ở Giai đoạn E.
SQLAlchemy==2.0.49
alembic==1.15.1
psycopg2-binary==2.9.9
python-dotenv==1.0.1
