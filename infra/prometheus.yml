global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
  # Backend FastAPI expose /metrics (prometheus-fastapi-instrumentator), chạy trong profile "app"
  - job_name: backend
    static_configs:
      - targets: ["backend:8000"]
